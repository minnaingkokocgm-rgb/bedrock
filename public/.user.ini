; Used by PHP-FPM / CGI hosts that honor .user.ini next to the front controller.
; Sized for ~800MB–1GB video uploads (post_max must be >= upload_max).
upload_max_filesize = 1024M
post_max_size = 1100M
memory_limit = 1024M
max_execution_time = 1800
max_input_time = 1800
